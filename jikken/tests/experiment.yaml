# Experiment Parameters
input_parameters:
    filepath: /data
    preprocessing: true
    batch_size: 4
    transformations:
      - stopwords
      - tokenize
      - remove_punct
training_parameters:
    algorithm: Seq2Seq
    batch_size: 100
    attention: multiplicative
